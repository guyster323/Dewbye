# 네트워크 문제 해결 가이드

## 🚨 현재 상황

회사 네트워크 환경에서 Gradle이 필요한 파일을 다운로드할 때 SSL 인증서 오류가 발생하고 있습니다.

### 발생한 오류
```
javax.net.ssl.SSLHandshakeException: PKIX path building failed
```

이는 회사 방화벽이나 프록시 서버가 SSL 인증서를 가로채거나 차단하기 때문입니다.

---

## ✅ 해결 방법

### 방법 1: Android Studio에서 빌드 (가장 권장)

1. **Android Studio 실행**

2. **프로젝트 열기**
   - File → Open...
   - `D:\LGES_Backup\AI_Driven\Dewbye\Dewbye\dewbye` 선택

3. **Gradle 동기화**
   - 프로젝트가 열리면 자동으로 Gradle 동기화가 시작됩니다
   - 상단에 "Gradle Sync" 알림이 나타나면 "Sync Now" 클릭

4. **기기 선택**
   - 상단 툴바에서 연결된 기기 (SM F946N) 선택

5. **실행**
   - 초록색 재생 버튼 클릭 또는 Shift + F10

### 방법 2: VPN 또는 다른 네트워크 사용

회사 네트워크가 아닌 다른 네트워크에 연결:
- 개인 핫스팟 (스마트폰)
- 가정용 Wi-Fi
- VPN 연결

네트워크 변경 후:
```powershell
cd D:\LGES_Backup\AI_Driven\Dewbye\Dewbye\dewbye
flutter clean
flutter pub get
flutter run -d R3CW80CCH6V
```

### 방법 3: 네트워크 관리자에게 문의

IT 부서에 다음 URL에 대한 접근 권한 요청:
- `https://services.gradle.org`
- `https://repo.maven.apache.org`
- `https://plugins.gradle.org`
- `https://jcenter.bintray.com`

### 방법 4: 프록시 설정 (회사에서 프록시 사용 시)

`android/gradle.properties`에 프록시 설정 추가:
```properties
systemProp.http.proxyHost=your.proxy.host
systemProp.http.proxyPort=8080
systemProp.https.proxyHost=your.proxy.host
systemProp.https.proxyPort=8080
systemProp.http.proxyUser=username
systemProp.http.proxyPassword=password
systemProp.https.proxyUser=username
systemProp.https.proxyPassword=password
```

### 방법 5: Gradle 오프라인 모드 (권장하지 않음)

```powershell
cd android
.\gradlew assembleDebug --offline
```

---

## 📱 대안: Android Studio를 통한 완전한 빌드 과정

### 1단계: 프로젝트 설정
1. Android Studio 실행
2. File → Open → `dewbye` 폴더 선택
3. Gradle 동기화 완료까지 대기 (수 분 소요 가능)

### 2단계: SDK 확인
1. File → Settings → Appearance & Behavior → System Settings → Android SDK
2. 필요한 SDK 및 Build Tools 설치 확인:
   - Android 15 (API 35)
   - Android SDK Build-Tools
   - Google Play Services

### 3단계: 기기 연결 확인
1. View → Tool Windows → Device Manager
2. SM F946N이 표시되는지 확인

### 4단계: 빌드 및 실행
1. Build → Make Project (Ctrl + F9)
2. Run → Run 'main.dart' (Shift + F10)

### 5단계: APK 생성
1. Build → Flutter → Build APK
2. APK 생성 위치: `build/app/outputs/flutter-apk/app-release.apk`

---

## 🔧 추가 문제 해결

### Gradle Daemon 중지
```powershell
cd android
.\gradlew --stop
cd ..
flutter clean
```

### Gradle 캐시 완전 삭제
```powershell
Remove-Item -Path "$env:USERPROFILE\.gradle\caches" -Recurse -Force
Remove-Item -Path "$env:USERPROFILE\.gradle\wrapper" -Recurse -Force
```

### Flutter 캐시 삭제
```powershell
flutter clean
flutter pub cache repair
flutter pub get
```

---

## 💡 빠른 해결책 요약

**가장 빠른 방법: Android Studio 사용**

1. Android Studio 실행
2. `D:\LGES_Backup\AI_Driven\Dewbye\Dewbye\dewbye` 열기
3. Gradle 동기화 완료 대기
4. 상단의 Run 버튼 클릭

**또는 다른 네트워크로 변경 후:**

```powershell
cd D:\LGES_Backup\AI_Driven\Dewbye\Dewbye\dewbye
flutter clean
flutter run -d R3CW80CCH6V
```

---

## 📞 지원

Android Studio에서도 같은 문제가 발생하면:
1. IT 부서에 Gradle/Maven 저장소 접근 권한 요청
2. 개인 네트워크 (핫스팟)로 변경
3. VPN 사용

**네트워크 문제가 해결되면 자동으로 빌드가 성공할 것입니다.**



