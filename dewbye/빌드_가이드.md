# Dewbye 앱 빌드 완료 안내

## ✅ 완료된 작업

### 1. Intro Screen 추가 완료
- **배경**: Intro.mp4 비디오가 60% 투명도로 반복 재생
- **권한 상태 표시**: 위치, 저장소 권한 상태를 표시하고 자동으로 권한 요청
- **사용자 입력 위젯**:
  - 📍 위치 선택 (현재 위치 자동 감지 또는 수동 선택)
  - 🏢 건물 타입 선택 (주거용, 상업용, 산업용, 창고, 사무실)
  - 🌡️ 실내 온도 설정 (10°C ~ 30°C 슬라이더)
  - 💧 실내 습도 설정 (20% ~ 80% 슬라이더)

### 2. 앱 아이콘 변경
- Dewbye_App_Icon2.png를 앱 아이콘으로 설정
- flutter_launcher_icons 패키지로 모든 해상도의 아이콘 자동 생성

### 3. 필요한 권한 추가 (AndroidManifest.xml)
- 위치 권한 (ACCESS_FINE_LOCATION, ACCESS_COARSE_LOCATION)
- 저장소 권한 (Android 버전별 대응)
- 인터넷 권한

### 4. 패키지 추가
- `video_player`: 비디오 재생
- `permission_handler`: 권한 관리

---

## 🚀 빌드 방법

### 방법 1: 자동 빌드 스크립트 사용 (권장)

새 PowerShell 터미널을 열고 다음 명령을 실행하세요:

```powershell
cd D:\LGES_Backup\AI_Driven\Dewbye\Dewbye\dewbye
.\build_and_deploy.bat
```

### 방법 2: 수동 단계별 빌드

새 PowerShell 터미널을 **관리자 권한**으로 열고:

```powershell
# 1. 프로젝트 폴더로 이동
cd D:\LGES_Backup\AI_Driven\Dewbye\Dewbye\dewbye

# 2. Flutter 패키지 설치
flutter pub get

# 3. 앱 아이콘 생성
flutter pub run flutter_launcher_icons

# 4. Android 기기 연결 확인
flutter devices

# 5. APK 빌드 (Release)
flutter build apk --release

# 6. 연결된 Android 기기에 설치
flutter install
```

### 방법 3: VS Code에서 실행

1. VS Code에서 `dewbye` 폴더 열기
2. `lib/main.dart` 파일 열기
3. Android 기기를 USB로 연결
4. F5 키를 눌러 디버그 실행
5. 또는 Ctrl+Shift+P → "Flutter: Run Flutter Doctor" 실행 후 빌드

---

## 📱 Android 기기 준비

### 1. 개발자 옵션 활성화
- **설정** → **휴대전화 정보** → **빌드 번호**를 7번 연속 터치

### 2. USB 디버깅 활성화
- **설정** → **개발자 옵션** → **USB 디버깅** 켜기

### 3. USB로 컴퓨터에 연결
- 연결 시 "USB 디버깅 허용" 팝업에서 **"허용"** 선택

### 4. 연결 확인
```powershell
flutter devices
```

출력 예시:
```
Android SDK built for x86 (mobile) • emulator-5554 • android-x86 • Android 11 (API 30)
SM-G981N (mobile) • R3CN1234567 • android-arm64 • Android 13 (API 33)
```

---

## 🔧 문제 해결

### ❌ "flutter 명령어를 찾을 수 없습니다"

**해결 방법 1**: PowerShell 재시작
1. 현재 PowerShell 창 닫기
2. 새 PowerShell 창 열기 (관리자 권한)
3. 다시 명령어 실행

**해결 방법 2**: Flutter 경로 확인
```powershell
# Flutter 경로 확인
where.exe flutter

# Flutter가 설치되어 있지만 경로에 없다면:
$env:Path += ";C:\src\flutter\bin"  # Flutter 설치 경로로 변경

# 또는 Flutter SDK 재설치
# https://docs.flutter.dev/get-started/install/windows
```

### ❌ "기기를 찾을 수 없습니다"

1. USB 케이블이 데이터 전송을 지원하는지 확인
2. USB 디버깅이 활성화되어 있는지 확인
3. Google USB Driver 설치:
   ```powershell
   # Android Studio SDK Manager에서 설치
   # Tools → SDK Manager → SDK Tools → Google USB Driver
   ```
4. 기기 제조사의 USB 드라이버 설치 (Samsung, LG 등)

### ❌ "Gradle 빌드 실패"

```powershell
cd android
.\gradlew clean
cd ..
flutter clean
flutter pub get
flutter build apk --release
```

### ❌ "앱 아이콘이 변경되지 않음"

```powershell
flutter pub run flutter_launcher_icons
flutter clean
flutter build apk --release
```

---

## 📦 빌드 결과물

빌드가 완료되면 APK 파일이 생성됩니다:

```
D:\LGES_Backup\AI_Driven\Dewbye\Dewbye\dewbye\build\app\outputs\flutter-apk\app-release.apk
```

이 APK 파일을:
- 다른 Android 기기에 복사하여 직접 설치 가능
- Google Play Console에 업로드하여 배포 가능
- 이메일이나 메신저로 전송하여 공유 가능

---

## 📋 체크리스트

실행하기 전에 확인하세요:

- [ ] Flutter SDK가 설치되어 있음
- [ ] Android Studio 또는 Android SDK가 설치되어 있음
- [ ] Android 기기가 USB로 연결되어 있음
- [ ] USB 디버깅이 활성화되어 있음
- [ ] 새 PowerShell 창에서 실행
- [ ] 관리자 권한으로 실행 (권장)

---

## 💡 추가 정보

### 디버그 모드로 실행 (개발 중)
```powershell
flutter run
```

### 로그 확인
```powershell
flutter logs
```

### 앱 성능 프로파일링
```powershell
flutter run --profile
```

### APK 크기 분석
```powershell
flutter build apk --analyze-size
```

---

## 📞 지원이 필요하신가요?

Flutter 관련 문서:
- 공식 문서: https://docs.flutter.dev/
- 한글 문서: https://flutter-ko.dev/

명령어가 실행되지 않으면:
1. 새 터미널 열기
2. PowerShell 관리자 권한으로 실행
3. `flutter doctor` 명령으로 환경 확인
4. VS Code의 Flutter extension 설치 확인

---

**작성일**: 2025-12-02  
**버전**: 1.0.0




